Course Wrap Up

-   Hibernate/JPA provides a new approach for data persistence
-   Standardize the persistence layer
-   JPA vs Hibernate
    -   JPA is the specification and it defines the API for the ORM
    -   Hibernate is the implementation of the JPA specification. It also provides additional functionality not
        provided in the specification.

-   Spring pairs very well with Hibernate