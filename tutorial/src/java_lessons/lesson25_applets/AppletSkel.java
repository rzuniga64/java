package java_lessons.lesson25_applets;

import java.applet.*;

public class AppletSkel extends Applet {
    /*
    init();     // initialize variables
    start();    // processing before applet is visible.
    paint(g);   // primary method where most of the executable code is located
    update();   // use when applet needs to be udpated
    stop ();    // used when an applet leaves a page and processing needs
                // to occur when an applet is not current
    destroy();  // used to release any resources that was accumulated
                // while an applet was running. Such as when a stop
                // button is pressed and you are connected to a database
    */
}
